# Backend API
WRKOUT_API_KEY=
API_PORT=8787
API_HOST=0.0.0.0

# Frontend
# Usado por vite.config.ts para proxy dev de /api
VITE_DEV_API_TARGET=http://localhost:8787
# Opcional: URL absoluta en build de frontend (si backend no comparte dominio)
# VITE_API_BASE_URL=https://tu-backend.tld/api
